declare type SupportedPlatforms = 'shopify' | 'bigcommerce' | 'api';
interface Props {
    readonly siteId: string;
    readonly email: string;
    readonly variant: string;
    readonly product: string;
    readonly platform: SupportedPlatforms;
    readonly listId?: string;
    readonly shouldSubscribe?: boolean;
    readonly hostname?: string;
    readonly abortSignal?: AbortSignal;
}
export declare function subscribeToBackInStock({ siteId, email, variant, product, listId, shouldSubscribe, platform, hostname, abortSignal, }: Props): Promise<Response>;
export {};
